<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.tuum.mapper.TransactionMapper">

    <resultMap id="TransactionResultMap" type="org.example.tuum.entity.Transaction">
        <id     property="id"           column="id"/>
        <result property="accountId"    column="account_id"/>
        <result property="amount"       column="amount"/>
        <result property="currency"     column="currency"
                javaType="org.example.tuum.entity.Currency"
                typeHandler="org.apache.ibatis.type.EnumTypeHandler"/>
        <result property="direction"    column="direction"
                javaType="org.example.tuum.entity.Direction"
                typeHandler="org.apache.ibatis.type.EnumTypeHandler"/>
        <result property="description"  column="description"/>
        <result property="balanceAfter" column="balance_after"/>
        <result property="createdAt"    column="created_at"/>
    </resultMap>

    <insert id="insert" parameterType="org.example.tuum.entity.Transaction"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO transactions (account_id, amount, currency, direction, description, balance_after)
        VALUES (
            #{accountId},
            #{amount},
            #{currency,   typeHandler=org.apache.ibatis.type.EnumTypeHandler},
            #{direction,  typeHandler=org.apache.ibatis.type.EnumTypeHandler},
            #{description},
            #{balanceAfter}
        )
    </insert>

    <select id="findByAccountId" resultMap="TransactionResultMap">
        SELECT id, account_id, amount, currency, direction, description, balance_after, created_at
        FROM transactions
        WHERE account_id = #{accountId}
        ORDER BY created_at DESC
    </select>

</mapper>
